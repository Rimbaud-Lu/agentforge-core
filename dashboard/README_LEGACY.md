# Legacy Module Notice

This root-level directory is considered transitional / legacy during package cleanup.

Preferred implementation location:
- `agentforge_core/`

Do not place new primary implementation here unless you are intentionally maintaining backward compatibility.
