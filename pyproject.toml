[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "agentforge-core"
version = "0.9.0"
description = "AI Agent Software Factory prototype"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "openai",
    "qdrant-client",
    "redis",
    "prometheus-client",
    "kafka-python",
    "pyyaml",
    "python-dotenv"
]

[project.optional-dependencies]
dev = ["pytest"]

[project.scripts]
agentforge = "agentforge_core.cli.cli:main"

[tool.setuptools]
include-package-data = true

[tool.setuptools.packages.find]
where = ["."]
include = ["agentforge_core*", "control_plane*", "router*", "workflow*", "observability*", "distributed*", "model_execution*", "memory*", "agents*", "skills*", "task_graph*", "planning*", "interface*", "tools*"]
