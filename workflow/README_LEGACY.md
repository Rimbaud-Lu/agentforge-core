# Legacy Module Notice

This root-level directory is transitional.

Current policy:
- Keep it only while imports still depend on it
- Move new implementation to `agentforge_core/`
- Remove this directory after import migration and tests pass
